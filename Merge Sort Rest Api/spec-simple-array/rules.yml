---
kind : rule
name : R1
service : S
actions :
 - (c1,tab) = __call S1(a,b,c)
 - tab1 = tab
 - res = __call S2(a,tab1)
---
kind : rule
name : R2
service : S1
actions :
 - tab[a] = a
 - tab[b] = a
 - tab[c] = a
 - res = tab[b]

---
kind : rule
name : R3
service : S2
guard : 
 classPath: com.local.Util
 method: guardArray
actions :
# - tab1[a] = a
 - b = a
